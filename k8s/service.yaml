apiVersion: v1
kind: Service
metadata:
  name: payments-service
  namespace: default
spec:
  selector:
    app: payments-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP